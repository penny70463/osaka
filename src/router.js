import Vue from 'vue';
import router from 'vue-router';


Vue.use(router);

export default new router({
	mode: 'history',
	routes: [
		// {
		// 	path: '',
		// 	name: '',
		// 	component: NearBy,

		// },
	],
});